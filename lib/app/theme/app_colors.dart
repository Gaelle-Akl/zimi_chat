import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const primary = Color(0xFFFF5A52);
  static const primaryDark = Color(0xFFF0443D);
  static const primaryLight = Color(0xFFFF8A83);

  // Background
  static const background = Color(0xFFFFF9F8);
  static const surface = Color(0xFFFFFFFF);
  

  // Text
  static const text = Color(0xFF172B3A);
  static const secondaryText = Color(0xFF718096);
  static const textLight =Color(0xFF9AA3AA);
  // Chat
  static const userBubble = Color(0xFFFF5A52);
  static const aiBubble = Color(0xFFFFFFFF);

  // Borders
  static const border = Color(0xFFDCE8F2);

  // Status
  static const success = Color(0xFF70D69B);
  static const warning = Color(0xFFF2B84B);
  static const error = Color(0xFFE66A6A);
}
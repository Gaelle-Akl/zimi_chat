import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const primary = Color(0xFF4A9FEF);
  static const primaryDark = Color(0xFF287FCF);
  static const primaryLight = Color(0xFFE8F4FF);

  // Background
  static const background = Color(0xFFF6FAFE);

  // Basic colors
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);

  // Text
  static const text = Color(0xFF172B3A);
  static const secondaryText = Color(0xFF718096);

  // Chat
  static const userBubble = Color(0xFFDCEEFF);
  static const aiBubble = Color(0xFFFFFFFF);

  // Borders
  static const border = Color(0xFFDCE8F2);

  // Status
  static const success = Color(0xFF4DBA8A);
  static const warning = Color(0xFFF2B84B);
  static const error = Color(0xFFE66A6A);
}